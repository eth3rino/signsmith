import { ParallaxBackground } from './parallax-background';

describe('ParallaxBackground', () => {
  it('should create an instance', () => {
    const directive = new ParallaxBackground();
    expect(directive).toBeTruthy();
  });
});
